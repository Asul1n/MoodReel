# 环境配置指南

> 权威配置以 **`docs/系统运行配置与AI提示词说明.md`** 与 **`backend/README.md`** 为准，本文档为速查。

## 1. 后端（Python 服务中枢）

| 项 | 要求 |
|----|------|
| Python | **3.10+** |
| 依赖 | `pip install -r requirements.txt -r requirements-dev.txt` |
| 训练依赖（成员B） | `pip install -r requirements-train.txt`（或 CPU 版 torch） |
| 数据库 | SQLite（`backend/data/`，路径见 `MOODREEL_DB`） |
| 模型产物 | `backend/models/vocab.json` + `textcnn.npz`（不入库） |

### 环境变量（`cp .env.example .env`）

| 变量 | 说明 |
|------|------|
| `MOODREEL_DB` | SQLite 路径，默认 `./data/moodreel.db` |
| `MOODREEL_MODEL_DIR` | 训练产物目录，默认 `./models` |
| `TENCENT_APPID` / `TENCENT_SECRET_KEY` / `TENCENT_ENABLED` | 腾讯情感 API（中文通道） |

### 数据准备与启动（一次）

```bash
cd backend
python scripts/download_imdb.py     # 下载/放置 IMDB 50k CSV 到 data/
python scripts/seed_db.py           # 建库 + 灌入静态语料 + 预置影片/离线样本
python scripts/train_textcnn.py     # 成员B：训练导出 models/（否则 model_ready=false）
uvicorn app.main:app --host 0.0.0.0 --port 8000
curl http://127.0.0.1:8000/health   # → {"ok":true,"model_ready":false,"version":"0.1.0"}
```

## 2. 前端 App（成员C）

| 项 | 要求 |
|----|------|
| IDE | DevEco Studio，新建 Empty Ability，语言 ArkTS，Compatible SDK ≥ API 10 |
| 工程位置 | `app/`（DevEco 生成的 AppScope/entry 等整体放入） |
| 网络权限 | `module.json5` 加 `ohos.permission.INTERNET` |
| 后端地址 | App「设置」页可配（存 Preferences），默认宿主机局域网 IP |

## 3. 密钥与合规

- 腾讯 `AppID/SecretKey` 放 `.env`，**不入库**。
- `*.db`、`backend/models/*.npz`、IMDB csv **不入库**（`.gitignore` 已配），大文件单独共享。
- 爬虫对目标站点低频率、只读公开数据，仅供教学分析。

## 4. 常见坑

| 现象 | 原因 | 解决 |
|------|------|------|
| `/health` 返回 `model_ready:false` | 未训练/未放模型 | 运行 `train_textcnn.py`，或放入 `models/textcnn.npz` |
| App 连不上后端 | 用了 `localhost` / 未开 INTERNET 权限 | 用宿主机局域网 IP + 声明权限 + 明文 http 放开 |
| 中文情感不可用 | 未配腾讯密钥 / 超时 | 配 `.env` 的 `TENCENT_*`；降级提示以 App 文案为准 |
| 爬虫失败 | 反爬/断网 | 走离线样本包（`crawler/sample_pack/`），状态 `degraded` |
